# Netlify configuration for the Vite (React) client living in /client

[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

# SPA routing: send all non-file requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
